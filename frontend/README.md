# AI Data Analyst Agent — Frontend

React + Vite + TypeScript chat UI for the [AI Data Analyst Agent](../README.md).
See the main project README for architecture, setup, and usage — this
package has no documentation of its own beyond what's in `package.json`'s
scripts (`dev`, `build`, `lint`, `test`).
